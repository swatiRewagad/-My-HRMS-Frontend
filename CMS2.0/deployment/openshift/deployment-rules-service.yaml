apiVersion: apps/v1
kind: Deployment
metadata:
  name: cms-rules-service
  namespace: rbi-cms
  labels:
    app: cms-rules-service
    app.kubernetes.io/part-of: rbi-cms
spec:
  replicas: 2
  selector:
    matchLabels:
      app: cms-rules-service
  template:
    metadata:
      labels:
        app: cms-rules-service
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "8080"
    spec:
      containers:
        - name: cms-rules-service
          image: image-registry.openshift-image-registry.svc:5000/rbi-cms/cms-rules-service:latest
          ports:
            - containerPort: 8080
          envFrom:
            - configMapRef:
                name: cms-common-config
            - secretRef:
                name: cms-db-secret
          env:
            - name: SPRING_PROFILES_ACTIVE
              value: "openshift"
          resources:
            requests:
              cpu: 200m
              memory: 384Mi
            limits:
              cpu: 500m
              memory: 768Mi
          readinessProbe:
            httpGet:
              path: /actuator/health/readiness
              port: 8080
            initialDelaySeconds: 30
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /actuator/health/liveness
              port: 8080
            initialDelaySeconds: 60
            periodSeconds: 15
---
apiVersion: v1
kind: Service
metadata:
  name: cms-rules-service
  namespace: rbi-cms
spec:
  selector:
    app: cms-rules-service
  ports:
    - port: 8080
      targetPort: 8080
  type: ClusterIP
