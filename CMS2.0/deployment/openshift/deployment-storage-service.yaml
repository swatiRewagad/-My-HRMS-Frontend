apiVersion: apps/v1
kind: Deployment
metadata:
  name: cms-storage-service
  namespace: rbi-cms
  labels:
    app: cms-storage-service
    app.kubernetes.io/part-of: rbi-cms
spec:
  replicas: 2
  selector:
    matchLabels:
      app: cms-storage-service
  template:
    metadata:
      labels:
        app: cms-storage-service
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "8080"
    spec:
      containers:
        - name: cms-storage-service
          image: image-registry.openshift-image-registry.svc:5000/rbi-cms/cms-storage-service:latest
          ports:
            - containerPort: 8080
          envFrom:
            - configMapRef:
                name: cms-common-config
            - secretRef:
                name: cms-db-secret
          env:
            - name: SPRING_PROFILES_ACTIVE
              value: "openshift"
          resources:
            requests:
              cpu: 200m
              memory: 384Mi
            limits:
              cpu: 500m
              memory: 768Mi
          readinessProbe:
            httpGet:
              path: /actuator/health/readiness
              port: 8080
            initialDelaySeconds: 30
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /actuator/health/liveness
              port: 8080
            initialDelaySeconds: 60
            periodSeconds: 15
          volumeMounts:
            - name: cms-storage
              mountPath: /cms-storage
      volumes:
        - name: cms-storage
          persistentVolumeClaim:
            claimName: cms-storage-pvc
---
apiVersion: v1
kind: Service
metadata:
  name: cms-storage-service
  namespace: rbi-cms
spec:
  selector:
    app: cms-storage-service
  ports:
    - port: 8080
      targetPort: 8080
  type: ClusterIP
